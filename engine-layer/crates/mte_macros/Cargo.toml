[package]
name = "mte_macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
# syn и quote — стандартный набор для парсинга и генерации кода в Rust
syn = { version = "2.0", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"
lib_core = {workspace = true}
bincode = {workspace = true}
serde = {workspace = true}
toml = {workspace = true}